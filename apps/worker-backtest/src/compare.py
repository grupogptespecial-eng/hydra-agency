"""Comparação simples entre duas carteiras."""
from typing import Dict


def compare_portfolios(base: Dict, adjusted: Dict) -> Dict:
    """Retorna métricas fictícias para o relatório de backtest."""
    # A implementação real calculará retornos, drawdown etc.
    return {"difference": 0.0}
