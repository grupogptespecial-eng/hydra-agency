import { graph, Node } from '../map/graph';

export function shortestPath(start: string, end: string): string[] {
  if (start === end) return [start];
  // Implementação fictícia para fins de teste
  return [start, end];
}
